#include <iostream>
#include <iomanip>
using namespace std;

int main(){
    int A, B, C, D, diferenca;
    

    cout << "A:" << endl;
    cin >> A;

    cout << "B:" << endl;
    cin >> B;

    cout << "C:" << endl;
    cin >> C;

    cout << "D:" << endl;
    cin >> D;

    diferenca = (A * B) - (C * D);

    cout << "Diferenca: " << diferenca << endl;
}